using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Nvp.Tools.Events
{
    //public interface IEventAggregator
    //{
    //    void DispatchEvent(string eventName, object eventArgs);

    //    void RegisterListener(string eventName, Action<object> callback);

    //    void UnegisterListener(string eventName, Action<object> callback);
        
    //    void DispatchEvent(GE eventName, object eventArgs);

    //    void RegisterListener(GE eventName, Action<object> callback);

    //    void UnegisterListener(GE eventName, Action<object> callback);
    //}
}
